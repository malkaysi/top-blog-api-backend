const express = require('express');
const router = express.Router();

exports.profile_get = (req, res) => {
  // Includes username, number of posts
  // Get all the posts where the userId is equivalent to the userId from url
  res.send('TODO: Develop the profile page')
}